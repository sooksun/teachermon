# 🎯 ออกแบบ Schema ตัวชี้วัดแบบ Sub-indicators

**วันที่สร้าง**: 27 มกราคม 2569  
**อ้างอิง**: INDICATOR_TABLE_DETAILED.md - ตัวเลือกที่ 3: ใช้ Sub-indicators

---

## 📋 สรุปการออกแบบ

ออกแบบระบบตัวชี้วัดแบบ **Hierarchical Structure** ที่รองรับ:
- **ตัวชี้วัดหลัก (Main Indicators)**: 8 ตัว (WP_1, WP_2, WP_3, ET_1, ET_2, ET_3, ET_4)
- **ตัวชี้วัดย่อย (Sub-indicators)**: 25 ตัว (WP_1.1, WP_1.2, ..., ET_P2.4)

**ข้อดี**:
- ✅ รองรับทั้งแบบย่อ (Main) และแบบละเอียด (Sub)
- ✅ Backward compatible กับข้อมูลเดิม
- ✅ ครูสามารถเลือกได้ทั้งแบบย่อหรือแบบละเอียด
- ✅ AI สามารถแนะนำได้ทั้ง 2 ระดับ

---

## 🗄️ Database Schema Design

### 1. ตาราง `Indicator` (ตัวชี้วัดหลัก)

```prisma
model Indicator {
  id          String   @id @default(uuid())
  code        String   @unique // WP_1, WP_2, ET_1, etc.
  name        String   // ชื่อตัวชี้วัด
  description String   @db.Text // คำอธิบาย
  category    String   // หมวดหมู่ (การพัฒนาการสอน, ความเป็นมืออาชีพ)
  aspect      String   // ด้าน (วิชาชีพ, สังคม, คุณลักษณะส่วนบุคคล)
  order       Int      // ลำดับการแสดงผล
  isActive    Boolean  @default(true)
  
  // Relations
  subIndicators SubIndicator[] // ตัวชี้วัดย่อย
  
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")
  
  @@map("indicators")
  @@index([code])
  @@index([aspect])
}
```

### 2. ตาราง `SubIndicator` (ตัวชี้วัดย่อย)

```prisma
model SubIndicator {
  id          String   @id @default(uuid())
  code        String   @unique // WP_1.1, WP_1.2, ET_P1.1, etc.
  name        String   // ชื่อตัวชี้วัดย่อย
  description String   @db.Text // คำอธิบาย
  requirements String  @db.Text // สิ่งที่ต้องปฏิบัติ
  assessmentMethods String? @db.Text // วิธีการประเมิน
  evidenceExamples Json? // ตัวอย่างเอกสารหลักฐาน (Array)
  order       Int      // ลำดับการแสดงผล
  
  // Relations
  indicatorId String   @map("indicator_id")
  indicator   Indicator @relation(fields: [indicatorId], references: [id], onDelete: Cascade)
  
  isActive    Boolean  @default(true)
  
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")
  
  @@map("sub_indicators")
  @@index([indicatorId])
  @@index([code])
}
```

### 3. อัปเดต `EvidencePortfolio`

```prisma
model EvidencePortfolio {
  // ... existing fields ...
  
  // เปลี่ยนจาก JSON array เป็น JSON object ที่รองรับทั้ง Main และ Sub
  indicatorCodes      Json      @map("indicator_codes") 
  // Format: 
  // {
  //   "main": ["WP_1", "ET_2"],           // ตัวชี้วัดหลัก
  //   "sub": ["WP_1.2", "WP_1.3", "ET_P1.4"] // ตัวชี้วัดย่อย (optional)
  // }
  // หรือแบบเก่า (backward compatible):
  // ["WP_1", "ET_2"] // ถ้าเป็น array แสดงว่าเป็น main indicators
  
  // ... rest of fields ...
}
```

---

## 📊 ข้อมูลเริ่มต้น (Seed Data)

### Main Indicators (8 ตัว)

| Code | Name | Category | Aspect |
|------|------|----------|--------|
| WP_1 | การออกแบบการจัดการเรียนรู้ | การพัฒนาการสอน | วิชาชีพ |
| WP_2 | การจัดการเรียนรู้ที่เน้นผู้เรียนเป็นสำคัญ | การพัฒนาการสอน | วิชาชีพ |
| WP_3 | การวัดและประเมินผล | การพัฒนาการสอน | วิชาชีพ |
| ET_1 | ความเป็นครู | ความเป็นมืออาชีพ | คุณลักษณะส่วนบุคคล |
| ET_2 | การจัดการชั้นเรียน | ความเป็นมืออาชีพ | คุณลักษณะส่วนบุคคล |
| ET_3 | ภาวะผู้นำทางวิชาการ | ความเป็นมืออาชีพ | สังคม |
| ET_4 | การพัฒนาตนเอง | ความเป็นมืออาชีพ | คุณลักษณะส่วนบุคคล |

### Sub-indicators (25 ตัว)

#### WP_1 → Sub-indicators (2 ตัว)
- **WP_1.1**: การวิเคราะห์หลักสูตร
- **WP_1.2**: การออกแบบการจัดการเรียนรู้

#### WP_2 → Sub-indicators (5 ตัว)
- **WP_1.3**: การจัดกิจกรรมการเรียนรู้
- **WP_1.4**: การเลือกและการใช้สื่อ เทคโนโลยี
- **WP_1.6**: การจัดบรรยากาศที่ส่งเสริมและพัฒนาผู้เรียน
- **WP_1.7**: การส่งเสริมการเรียนรู้โดยใช้เทคโนโลยีดิจิทัล
- **WP_2.1**: การจัดทำข้อมูลสารสนเทศของผู้เรียนและรายวิชา

#### WP_3 → Sub-indicators (3 ตัว)
- **WP_1.5**: การวัดและประเมินผล
- **WP_2.2**: การดำเนินการตามระบบดูแลช่วยเหลือผู้เรียน
- **WP_2.3**: การเรียนรู้ด้านกฎหมาย ระเบียบที่เกี่ยวข้อง

#### ET_1 → Sub-indicators (2 ตัว)
- **ET_P1.10**: การเป็นแบบอย่างที่ดี
- **ET_P1.11**: จรรยาบรรณวิชาชีพ

#### ET_2 → Sub-indicators (1 ตัว)
- **WP_1.6**: การจัดบรรยากาศที่ส่งเสริมและพัฒนาผู้เรียน (shared)

#### ET_3 → Sub-indicators (2 ตัว)
- **ET_S1**: การเรียนรู้จากกัลยาณมิตร
- **ET_S2**: การเรียนรู้ร่วมกันเป็นเครือข่ายทางวิชาชีพ (PLC)

#### ET_4 → Sub-indicators (10 ตัว)
- **ET_P1.1** ถึง **ET_P1.11**: การมีวินัย คุณธรรม จริยธรรมฯ (11 ตัว)
- **ET_P2.1** ถึง **ET_P2.4**: การพัฒนาตนเอง (4 ตัว)

---

## 🔄 Migration Strategy

### Phase 1: เพิ่มตารางใหม่ (Non-breaking)

1. สร้างตาราง `indicators` และ `sub_indicators`
2. Seed ข้อมูลตัวชี้วัดทั้งหมด
3. **ยังไม่เปลี่ยน** `evidence_portfolio.indicator_codes` (ยังใช้ JSON array)

### Phase 2: อัปเดต Frontend (Backward Compatible)

1. อัปเดต `IndicatorSelector` ให้รองรับทั้ง Main และ Sub
2. แสดงแบบ Tree/Hierarchical view
3. ยังรองรับการเลือกแบบเก่า (Main indicators only)

### Phase 3: Migration Data (Optional)

1. Script สำหรับ migrate ข้อมูลเดิม:
   ```typescript
   // Convert ["WP_1", "ET_2"] → {"main": ["WP_1", "ET_2"]}
   ```

### Phase 4: อัปเดต AI Services

1. อัปเดต AI prompts ให้แนะนำทั้ง Main และ Sub indicators
2. ใช้ Sub-indicators สำหรับการวิเคราะห์ที่ละเอียดขึ้น

---

## 💻 Frontend Design

### Component: `IndicatorSelector` (Enhanced)

```typescript
interface IndicatorSelectorProps {
  selectedCodes: {
    main?: string[];      // Main indicators
    sub?: string[];       // Sub-indicators (optional)
  };
  onSelectionChange: (codes: { main: string[]; sub?: string[] }) => void;
  mode?: 'main' | 'sub' | 'both'; // เลือกโหมดการแสดง
  aiSuggestedCodes?: {
    main?: string[];
    sub?: string[];
  };
}
```

### UI Design Options

#### Option 1: Tree View (Recommended)
```
☑ WP_1 - การออกแบบการจัดการเรียนรู้
  ☐ WP_1.1 - การวิเคราะห์หลักสูตร
  ☑ WP_1.2 - การออกแบบการจัดการเรียนรู้
☑ WP_2 - การจัดการเรียนรู้ที่เน้นผู้เรียนเป็นสำคัญ
  ☑ WP_1.3 - การจัดกิจกรรมการเรียนรู้
  ☐ WP_1.4 - การเลือกและการใช้สื่อ เทคโนโลยี
```

#### Option 2: Tabs View
```
[Main Indicators] [Sub-indicators]
```

#### Option 3: Collapsible Sections
```
▼ WP_1 - การออกแบบการจัดการเรียนรู้ (1/2 เลือก)
  ☑ WP_1.1 - การวิเคราะห์หลักสูตร
  ☐ WP_1.2 - การออกแบบการจัดการเรียนรู้
```

---

## 🤖 AI Integration

### 1. Evidence AI - Suggest Indicators

```typescript
// AI จะแนะนำทั้ง Main และ Sub
{
  "main": ["WP_1", "WP_2"],
  "sub": ["WP_1.2", "WP_1.3", "WP_1.4"]
}
```

### 2. Analyze Indicator Connection

```typescript
// วิเคราะห์ความเชื่อมโยงกับ Sub-indicators ที่เลือก
analyzeIndicatorConnection(
  evidenceId: string,
  selectedIndicators: {
    main: string[];
    sub: string[];
  }
)
```

---

## 📝 API Changes

### 1. GET `/indicators`
```typescript
// Response
{
  "main": [
    {
      "id": "...",
      "code": "WP_1",
      "name": "การออกแบบการจัดการเรียนรู้",
      "subIndicators": [...]
    }
  ]
}
```

### 2. POST `/evidence/upload`
```typescript
// Request body
{
  "indicatorCodes": {
    "main": ["WP_1"],
    "sub": ["WP_1.2"] // optional
  }
}
```

### 3. POST `/evidence/:id/analyze-connection`
```typescript
// Request body
{
  "indicatorCodes": {
    "main": ["WP_1"],
    "sub": ["WP_1.2", "WP_1.3"]
  }
}
```

---

## ✅ Benefits

1. **Backward Compatible**: ข้อมูลเดิมยังใช้งานได้
2. **Flexible**: ครูเลือกได้ทั้งแบบย่อหรือแบบละเอียด
3. **Scalable**: เพิ่ม Sub-indicators ได้ง่าย
4. **AI-Friendly**: AI สามารถแนะนำได้ทั้ง 2 ระดับ
5. **Reporting**: รายงานได้ทั้งแบบสรุป (Main) และแบบละเอียด (Sub)

---

## 🚀 Implementation Plan

### Step 1: Database Schema (1-2 วัน)
- [ ] สร้าง Prisma schema
- [ ] สร้าง migration
- [ ] Seed ข้อมูลตัวชี้วัด

### Step 2: Backend API (2-3 วัน)
- [ ] สร้าง `IndicatorsController` และ `IndicatorsService`
- [ ] อัปเดต `EvidenceService` ให้รองรับ format ใหม่
- [ ] อัปเดต AI services

### Step 3: Frontend Components (3-4 วัน)
- [ ] อัปเดต `IndicatorSelector` component
- [ ] สร้าง Tree/Collapsible UI
- [ ] อัปเดต Upload modal

### Step 4: Testing & Migration (2-3 วัน)
- [ ] ทดสอบ backward compatibility
- [ ] Migration script (optional)
- [ ] Integration testing

**รวมประมาณ**: 8-12 วัน

---

## 📚 Files to Create/Update

### New Files
- `packages/database/prisma/schema.prisma` (update)
- `apps/api/src/indicators/indicators.module.ts`
- `apps/api/src/indicators/indicators.controller.ts`
- `apps/api/src/indicators/indicators.service.ts`
- `apps/api/src/indicators/dto/indicator.dto.ts`
- `packages/database/prisma/seed-indicators.ts`

### Updated Files
- `apps/api/src/evidence/evidence.service.ts`
- `apps/api/src/evidence/evidence.controller.ts`
- `apps/api/src/ai/evidence-ai.service.ts`
- `apps/web/components/portfolio/indicator-selector.tsx`
- `apps/web/components/portfolio/upload-modal.tsx`

---

**สร้างเมื่อ**: 27 มกราคม 2569  
**อัปเดตล่าสุด**: 27 มกราคม 2569
