# 🤖 Gemini AI Setup - TeacherMon

## ✅ Setup เสร็จสมบูรณ์!

ระบบได้ตั้งค่า **Google Gemini AI** พร้อมใช้งานแล้ว

---

## 📝 การตั้งค่าที่ทำไปแล้ว

### 1. ติดตั้ง Package
```bash
pnpm add @google/generative-ai
```

### 2. ตั้งค่าใน `.env`
```env
GEMINI_API_KEY=AIzaSyCYRY9VGlZjCeYlNdMHXTUNK1qZshGUGDk
GEMINI_MODEL=gemini-2.0-flash-exp
AI_ENABLED=true
```

### 3. สร้าง Gemini AI Provider
ไฟล์: `apps/api/src/ai/providers/gemini-ai.provider.ts`

**Features:**
- ✅ `generateText()` - สร้าง text ทั่วไปจาก prompt
- ✅ `improveLanguage()` - ปรับภาษาให้เป็นทางการ
- ✅ `summarizeMentoringVisit()` - สรุปรายงานการนิเทศ
- ✅ `analyzeEvidence()` - วิเคราะห์หลักฐาน

### 4. อัปเดต AI Services
- ✅ `JournalAIService` → ใช้ Gemini แทน Mock
- ✅ `MentoringAIService` → ใช้ Gemini แทน Mock
- ✅ `EvidenceAIService` → ใช้ Gemini แทน Mock

---

## 🚀 วิธีใช้งาน

### ทดสอบ API

#### 1. ปรับภาษา Journal

```bash
curl -X POST http://localhost:3001/api/journals/ai/improve-language \
  -H "Content-Type: application/json" \
  -d '{
    "text": "วันนี้สอนเรื่องเศษส่วน เด็กๆ เข้าใจดี แต่บางคนยังสับสน",
    "indicatorCode": "WP.1"
  }'
```

**Response จาก Gemini:**
```json
{
  "improvedText": "ในวันนี้ได้จัดการเรียนรู้เรื่องเศษส่วน โดยภาพรวมนักเรียนสามารถเข้าใจแนวคิดเบื้องต้นได้เป็นอย่างดี อย่างไรก็ตาม ยังพบว่านักเรียนบางส่วนยังมีความสับสนในการเปรียบเทียบขนาดเศษส่วน ซึ่งต้องการการฝึกฝนเพิ่มเติม",
  "suggestions": [
    "ปรับปรุงภาษาให้เป็นทางการมากขึ้น",
    "เปลี่ยนจาก \"เด็ก\" เป็น \"นักเรียน\" เพื่อความเป็นทางการ",
    "พิจารณาเพิ่มตัวอย่างเฉพาะจากการเรียนการสอนจริง"
  ],
  "confidenceScore": 0.85,
  "model": "gemini-2.0-flash-exp"
}
```

#### 2. สร้างรายงานการนิเทศ

```bash
curl -X POST http://localhost:3001/api/mentoring/{visitId}/ai/generate-report
```

**Response จาก Gemini:**
```json
{
  "reportText": "รายงานการนิเทศการสอน\n\nวันที่: 24 มกราคม 2567\n...",
  "sections": {
    "summary": "การนิเทศครูปิยชนก พรหมศิลป์ ณ โรงเรียนบ้านดอยสูง พบว่าโดยรวมมีความพร้อมในการจัดการเรียนการสอนในระดับดี มีการเตรียมแผนการสอนที่ชัดเจนและครบถ้วน อย่างไรก็ตาม ยังมีประเด็นด้านการบริหารจัดการเวลาที่ควรปรับปรุง",
    "strengths": [
      "มีการเตรียมแผนการสอนที่มีโครงสร้างชัดเจน รวมถึงกำหนดวัตถุประสงค์และกิจกรรมไว้อย่างครบถ้วน",
      "ใช้สื่อการสอนที่หลากหลายและเหมาะสมกับระดับนักเรียน",
      "สร้างบรรยากาศในชั้นเรียนที่เอื้อต่อการเรียนรู้"
    ],
    "improvements": [
      "การบริหารจัดการเวลาในแต่ละกิจกรรมยังไม่เหมาะสม ทำให้บางกิจกรรมดำเนินการไม่ทันตามแผน",
      "การให้ข้อมูลย้อนกลับ (feedback) แก่นักเรียนควรเพิ่มเติมเพื่อช่วยให้นักเรียนพัฒนาได้ดียิ่งขึ้น"
    ],
    "followUpTasks": [
      "ปรับปรุงแผนการสอนโดยคำนึงถึงการจัดสรรเวลาให้เหมาะสมในแต่ละกิจกรรม",
      "ศึกษาเทคนิคการให้ข้อมูลย้อนกลับที่มีประสิทธิภาพ",
      "นัดหมายการติดตามผลในเดือนถัดไป"
    ]
  },
  "confidenceScore": 0.82
}
```

#### 3. วิเคราะห์หลักฐาน

```bash
curl -X POST http://localhost:3001/api/evidence \
  -H "Content-Type: application/json" \
  -d '{
    "teacherId": "teacher-123",
    "originalFilename": "แผนการสอนคณิตศาสตร์_ป3_เศษส่วน.pdf",
    "fileUrl": "https://...",
    "fileSize": 245760,
    "mimeType": "application/pdf"
  }'
```

**Response จาก Gemini:**
```json
{
  "standardFilename": "WP1_แผนการสอน_คณิตศาสตร์_ป.3_2026-01-24.pdf",
  "aiAnalysis": {
    "summary": "แผนการสอนวิชาคณิตศาสตร์ เรื่องเศษส่วน สำหรับนักเรียนชั้นประถมศึกษาปีที่ 3",
    "keywords": [
      "แผนการสอน",
      "คณิตศาสตร์",
      "เศษส่วน",
      "ป.3",
      "การออกแบบการเรียนรู้"
    ],
    "suggestedIndicators": ["WP.1", "WP.2"],
    "qualityCheck": {
      "completeness": 85,
      "recommendations": [
        "ตรวจสอบว่ามีวัตถุประสงค์การเรียนรู้ที่ชัดเจน",
        "ควรมีขั้นตอนการสอนที่เป็นลำดับ",
        "ต้องระบุวิธีการวัดและประเมินผล"
      ]
    }
  }
}
```

---

## 🔧 การตั้งค่าเพิ่มเติม

### เปลี่ยน Model

แก้ไขใน `.env`:
```env
GEMINI_MODEL=gemini-2.0-flash-exp  # รุ่นใหม่ล่าสุด (แนะนำ)
# หรือ
GEMINI_MODEL=gemini-1.5-flash      # รุ่นเสถียร
GEMINI_MODEL=gemini-1.5-pro        # รุ่นแม่นยำสูงสุด (ช้ากว่า)
```

### ปิด AI (ใช้ Mock แทน)

```env
AI_ENABLED=false
```

ระบบจะกลับไปใช้ rule-based mock responses แทน

---

## 📊 Gemini Models ที่แนะนำ

| Model | Speed | Quality | Cost | Use Case |
|-------|-------|---------|------|----------|
| **gemini-2.0-flash-exp** | ⚡⚡⚡ | ⭐⭐⭐⭐ | 💰 | **แนะนำ** - เร็ว แม่นยำ ราคาถูก |
| gemini-1.5-flash | ⚡⚡ | ⭐⭐⭐ | 💰 | Production (เสถียร) |
| gemini-1.5-pro | ⚡ | ⭐⭐⭐⭐⭐ | 💰💰💰 | งานซับซ้อน ต้องการความแม่นยำสูง |

---

## 🎯 AI Features ที่ใช้ Gemini

### 1. Journal AI
- ✅ ปรับภาษาให้เป็นทางการ
- ✅ คงความหมายเดิม
- ✅ ไม่ระบุชื่อนักเรียน (PDPA-safe)
- ✅ เพิ่มรายละเอียดให้สมบูรณ์

### 2. Mentoring AI
- ✅ สรุปการนิเทศเป็นรายงานราชการ
- ✅ แยกจุดเด่น/จุดพัฒนา
- ✅ สร้าง Follow-up tasks อัตโนมัติ
- ✅ ใช้ภาษาที่เป็นมาตรฐาน

### 3. Evidence AI
- ✅ วิเคราะห์ชื่อไฟล์
- ✅ แนะนำ Indicator (WP.1-4, ET.1-4)
- ✅ สร้างชื่อไฟล์มาตรฐาน
- ✅ ให้คำแนะนำคุณภาพ

---

## 🔒 Security & Privacy

### ข้อมูลที่ส่งไปยัง Gemini:
- ✅ ข้อความที่ผู้ใช้ป้อน (ผ่าน PDPA Scanner ก่อน)
- ✅ Context (Indicator, Focus Area)
- ❌ **ไม่ส่ง** ชื่อนักเรียน (ถูกแทนที่ด้วย "นักเรียน ก.")
- ❌ **ไม่ส่ง** ข้อมูลส่วนบุคคลอื่นๆ

### PDPA Protection:
1. ตรวจ PDPA **ก่อน** ส่งไปยัง Gemini
2. แทนที่ข้อมูลอ่อนไหวด้วย placeholder
3. Log ทุกการเรียกใช้ AI
4. Human review สำหรับเอกสารสำคัญ

---

## 📈 Monitoring & Logs

### ดู AI Usage:
```http
GET /api/ai/admin/activities/stats
```

### ตรวจสอบ Token Usage:
Gemini API มี Free Tier:
- 15 requests/minute
- 1,500 requests/day
- 1 million tokens/day (Free)

**ประมาณการ:**
- 1 Journal improvement ≈ 200-300 tokens
- 1 Mentoring report ≈ 400-600 tokens
- 1,000 AI requests/day ใช้ประมาณ 300,000 tokens

---

## ✅ Testing

### ทดสอบว่า Gemini ทำงาน:

```bash
cd apps/api
pnpm dev
```

ดู log:
```
[AIModule] Gemini AI initialized with model: gemini-2.0-flash-exp
```

### ทดสอบ API:
```bash
# Test 1: Improve Language
curl -X POST http://localhost:3001/api/journals/ai/improve-language \
  -H "Content-Type: application/json" \
  -d '{"text":"วันนี้สอนดี"}'

# ถ้าได้ response ที่มีความยาวมากกว่า input = Gemini ทำงาน! ✅
```

---

## 🐛 Troubleshooting

### ❌ Error: "GEMINI_API_KEY not configured"
**แก้:** ตรวจสอบว่ามี `.env` file และมี `GEMINI_API_KEY`

### ❌ Error: "Failed to initialize Gemini AI"
**แก้:** 
1. ตรวจสอบ API Key ถูกต้อง
2. ตรวจสอบ network connection
3. ลองใช้ model อื่น (gemini-1.5-flash)

### ❌ Response ช้า
**แก้:**
- เปลี่ยนเป็น `gemini-2.0-flash-exp` (เร็วที่สุด)
- ลดความยาว prompt
- ตรวจสอบ network latency

### ⚠️ AI Response ไม่ดี
**แก้:**
- เพิ่มรายละเอียดใน prompt
- ใช้ `gemini-1.5-pro` (แม่นยำกว่า)
- ปรับ system prompt ใน `gemini-ai.provider.ts`

---

## 🎉 เสร็จสมบูรณ์!

ระบบพร้อมใช้งาน Gemini AI แล้ว! 🚀

**Next Steps:**
1. ✅ Test API endpoints
2. ✅ สร้าง Frontend UI
3. ✅ Monitor usage & costs
4. ✅ Fine-tune prompts ตามความต้องการ

---

**หมายเหตุ:** API Key นี้อยู่ใน `.env` (ไม่ commit ไป Git)
