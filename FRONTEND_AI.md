# 🎨 Frontend AI Features

## ✅ สิ่งที่สร้างเสร็จแล้ว

### 1. AI Journal Helper Component
📁 `apps/web/components/ai/ai-journal-helper.tsx`

**Features:**
- ✨ **ปุ่มช่วยปรับภาษา** - เรียก Gemini AI ปรับภาษาให้เป็นทางการ
- 💡 **ปุ่มคำถามสะท้อนคิด** - แสดงคำถามตาม Indicator (WP.1-4, ET.1-4)
- 🔒 **ปุ่มตรวจ PDPA** - ตรวจสอบข้อมูลอ่อนไหวก่อนบันทึก

**การใช้งาน:**
```tsx
import { AIJournalHelper } from '@/components/ai/ai-journal-helper';

<AIJournalHelper 
  text={formData.reflectionText}
  onTextImproved={(improved) => setFormData(prev => ({ 
    ...prev, 
    reflectionText: improved 
  }))}
  indicatorCode="WP.1"
/>
```

### 2. Journal New Page (อัปเดตแล้ว)
📁 `apps/web/app/journals/new/page.tsx`

**การเพิ่ม:**
- ✅ เพิ่ม AI Helper ใต้ช่อง "การสะท้อนตนเอง"
- ✅ เชื่อมต่อกับ AI API
- ✅ แสดง Toast notifications สำหรับ feedback

---

## 🎯 การทำงานของ AI Features

### 1. ปรับภาษาอัตโนมัติ (✨)

**Flow:**
1. ครูเขียนข้อความสั้นๆ เช่น "วันนี้สอนเรื่องเศษส่วน เด็กๆ เข้าใจดี"
2. คลิกปุ่ม "✨ ช่วยปรับภาษา"
3. AI (Gemini) ปรับภาษา → "ในวันนี้ได้จัดการเรียนรู้เรื่องเศษส่วน..."
4. ข้อความถูกแทนที่ใน textarea อัตโนมัติ
5. แสดง Toast พร้อมคำแนะนำ

**UI/UX:**
- Loading state: ปุ่มเปลี่ยนเป็น "กำลังปรับภาษา..." พร้อม spinner
- Success: Toast สีเขียว "✨ ปรับภาษาเสร็จแล้ว!"
- Suggestions: Toast แสดงคำแนะนำเพิ่มเติม (ด้านล่างขวา)

---

### 2. คำถามสะท้อนคิด (💡)

**Flow:**
1. คลิกปุ่ม "💡 คำถามสะท้อนคิด"
2. แสดงกล่องสีฟ้าพร้อมคำถาม 4-5 ข้อ
3. ครูอ่านและใช้เป็นแนวทางในการเขียน

**ตัวอย่างคำถาม (WP.1):**
- วันนี้ฉันออกแบบกิจกรรมการเรียนรู้อย่างไร?
- สื่อและแหล่งเรียนรู้ที่ใช้วันนี้เหมาะสมหรือไม่?
- นักเรียนมีส่วนร่วมในการออกแบบกิจกรรมในระดับใด?

**UI/UX:**
- แสดงเป็นกล่องสีฟ้าใต้ปุ่ม
- มีปุ่ม X สำหรับปิด
- คำถามเรียงเป็นลิสต์

---

### 3. ตรวจสอบ PDPA (🔒)

**Flow:**
1. ครูเขียนข้อความเสร็จ
2. คลิก "🔒 ตรวจ PDPA" ก่อนบันทึก
3. AI ตรวจหาข้อมูลอ่อนไหว:
   - ชื่อ-นามสกุลนักเรียน
   - เลขประจำตัว
   - เบอร์โทรศัพท์
   - ที่อยู่
4. แสดงผลตรวจสอบ

**UI/UX:**

**✅ ปลอดภัย:**
```
Toast สีเขียว: "✅ ปลอดภัย! ไม่พบข้อมูลอ่อนไหว"
```

**⚠️ พบความเสี่ยง:**
```
Toast สีส้ม/แดง:
"⚠️ พบข้อมูลอ่อนไหว (2 รายการ)
• STUDENT_FULL_NAME: ใช้ "นักเรียน ก." แทน
• PHONE_NUMBER: ไม่ควรระบุเบอร์โทรศัพท์นักเรียน

คำแนะนำ: ตรวจสอบให้แน่ใจว่าไม่มีข้อมูลส่วนบุคคล..."
```

---

## 🎨 Design System

### Colors
- **ปรับภาษา (Purple):** `bg-purple-50 text-purple-700 border-purple-200`
- **คำถามสะท้อนคิด (Blue):** `bg-blue-50 text-blue-700 border-blue-200`
- **ตรวจ PDPA (Green):** `bg-green-50 text-green-700 border-green-200`

### Icons (lucide-react)
- `Sparkles` - ปรับภาษา
- `Lightbulb` - คำถามสะท้อนคิด
- `Shield` - ตรวจ PDPA
- `Loader2` - Loading state

### Button Styles
```tsx
className="inline-flex items-center px-3 py-1.5 text-sm font-medium 
  text-{color}-700 bg-{color}-50 border border-{color}-200 
  rounded-md hover:bg-{color}-100 transition-colors 
  disabled:opacity-50 disabled:cursor-not-allowed"
```

---

## 📱 Responsive Design

### Desktop (≥768px)
- ปุ่มอยู่แนวนอน 3 ปุ่ม
- กล่องคำถามแสดงเต็มความกว้าง

### Mobile (<768px)
- ปุ่มแสดง 1-2 ปุ่มต่อบรรทัด
- ข้อความลดขนาดเล็กลง

---

## 🔄 State Management

### Local State
```tsx
const [isImproving, setIsImproving] = useState(false);
const [isChecking, setIsChecking] = useState(false);
const [showPrompts, setShowPrompts] = useState(false);
const [prompts, setPrompts] = useState<string[]>([]);
```

### Loading States
- `isImproving` - ปรับภาษากำลังทำงาน
- `isChecking` - ตรวจ PDPA กำลังทำงาน

### Data States
- `prompts` - คำถามสะท้อนคิดที่ได้จาก API
- `showPrompts` - แสดง/ซ่อนกล่องคำถาม

---

## 🚀 API Calls

### 1. Improve Language
```typescript
await apiClient.post('/journals/ai/improve-language', {
  text: 'วันนี้สอนเรื่องเศษส่วน...',
  indicatorCode: 'WP.1',
});
```

**Response:**
```json
{
  "improvedText": "ในวันนี้ได้จัดการเรียนรู้...",
  "suggestions": ["ปรับปรุงภาษา...", "..."],
  "confidenceScore": 0.85,
  "model": "gemini-2.0-flash-exp"
}
```

### 2. Suggest Prompts
```typescript
await apiClient.post('/journals/ai/suggest-prompts', {
  indicatorCode: 'WP.1',
});
```

**Response:**
```json
{
  "prompts": [
    "วันนี้ฉันออกแบบกิจกรรม...",
    "สื่อและแหล่งเรียนรู้...",
    "..."
  ],
  "relatedIndicators": ["WP.2", "WP.3"],
  "focusAreas": ["การออกแบบการเรียนรู้"]
}
```

### 3. Check PDPA
```typescript
await apiClient.post('/journals/ai/check-pdpa', {
  text: 'เด็กชายสมชาย ใจดี...',
});
```

**Response:**
```json
{
  "isSafe": false,
  "riskLevel": "HIGH_RISK",
  "violations": [
    {
      "type": "STUDENT_FULL_NAME",
      "matchedText": "เด็กชายสมชาย ใจดี",
      "suggestion": "ใช้ \"นักเรียน ก.\" แทน"
    }
  ],
  "suggestions": ["ตรวจสอบให้แน่ใจ..."]
}
```

---

## ✨ User Experience Flow

### Happy Path
```
1. ครูเขียนข้อความสั้นๆ
   ↓
2. คลิก "✨ ช่วยปรับภาษา"
   ↓
3. รอ 2-3 วินาที (แสดง spinner)
   ↓
4. ข้อความถูกแทนที่อัตโนมัติ
   ↓
5. อ่าน suggestions
   ↓
6. (Optional) ปรับแก้เพิ่มเติม
   ↓
7. คลิก "🔒 ตรวจ PDPA"
   ↓
8. ได้ "✅ ปลอดภัย!"
   ↓
9. คลิก "บันทึก"
```

### Error Handling
- ❌ **No text:** Toast "กรุณาเขียนข้อความอย่างน้อย 10 ตัวอักษร"
- ❌ **API Error:** Toast "เกิดข้อผิดพลาด..."
- ❌ **Network Error:** Toast "ไม่สามารถเชื่อมต่อ API..."

---

## 📚 ตัวอย่างการใช้งานจริง

### Before AI:
```
"วันนี้สอนเรื่องเศษส่วน เด็กๆ เข้าใจดี แต่บางคนยังสับสน"
```

### After AI (Improved):
```
"ในวันนี้ได้จัดการเรียนรู้เรื่องเศษส่วน โดยภาพรวมนักเรียนสามารถเข้าใจ
แนวคิดเบื้องต้นได้เป็นอย่างดี อย่างไรก็ตาม ยังพบว่านักเรียนบางส่วน
ยังมีความสับสนในการเปรียบเทียบขนาดเศษส่วน ซึ่งต้องการการฝึกฝนเพิ่มเติม
โดยภาพรวมการจัดการเรียนรู้เป็นไปตามวัตถุประสงค์ที่กำหนดไว้"
```

---

## 🔜 Next Features (Future)

### For Journal
- [ ] AI แนะนำ Success Story จากข้อมูลเดิม
- [ ] แนะนำ Support Request ตามปัญหาที่พบ
- [ ] สรุป Trends รายเดือน

### For Mentoring
- [ ] ปุ่ม "สร้างรายงานอัตโนมัติ" ในหน้า Mentoring Detail
- [ ] แนะนำ Follow-up Tasks อัตโนมัติ

### For Evidence
- [ ] หน้าอัปโหลดหลักฐาน + AI แนะนำ Indicator
- [ ] ตั้งชื่อไฟล์มาตรฐานอัตโนมัติ
- [ ] ตรวจคุณภาพหลักฐาน

---

## 🧪 การทดสอบ

### Manual Testing
1. เปิด http://localhost:3000/journals/new
2. เขียนข้อความใน "การสะท้อนตนเอง"
3. ทดสอบปุ่มทั้ง 3:
   - ✨ ช่วยปรับภาษา
   - 💡 คำถามสะท้อนคิด
   - 🔒 ตรวจ PDPA

### Test Cases
```
✅ ข้อความสั้น (< 10 chars) → แสดง warning
✅ ข้อความปกติ → ปรับภาษาได้
✅ มีชื่อนักเรียน → PDPA ตรวจพบ
✅ ไม่มีข้อมูลอ่อนไหว → PDPA ผ่าน
✅ Network error → แสดง error toast
```

---

## 📦 Dependencies

```json
{
  "lucide-react": "^0.x.x",  // Icons
  "react-toastify": "^10.x.x",  // Toast notifications
  "@tanstack/react-query": "^5.x.x"  // Data fetching
}
```

---

## ✅ Checklist

- [x] สร้าง AIJournalHelper component
- [x] เพิ่ม AI features ใน Journal New page
- [x] เชื่อมต่อ API endpoints
- [x] Loading states & Error handling
- [x] Toast notifications
- [ ] เพิ่ม AI features ใน Mentoring page
- [ ] สร้างหน้า Evidence Portfolio
- [ ] สร้าง Admin Dashboard
- [ ] Responsive testing
- [ ] Unit tests

---

**หมายเหตุ:** Frontend UI สำหรับ Journal เสร็จแล้ว! ส่วน Mentoring และ Evidence จะทำในขั้นตอนต่อไป
