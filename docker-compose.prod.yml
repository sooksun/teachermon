# ============================================
# TeacherMon - Production Docker Compose
# Server: Ubuntu @ /DATA/AppData/www/rukthin
# Database: External MariaDB 11.4 @ 192.168.1.4
# Ports: API=9904, Web=9903 (ไม่ชนกับ CasaOS)
# ============================================
# Usage:
#   cd /DATA/AppData/www/rukthin
#   docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build
# ============================================

services:
  # ---- NestJS API ----
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    image: teachermon-api:latest
    container_name: teachermon-api
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      PORT: 3001
    volumes:
      - ./uploads:/app/apps/api/uploads
    ports:
      - '9904:3001'
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    restart: always
    networks:
      - teachermon-net
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:3001/api/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '5'

  # ---- Next.js Web ----
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://192.168.1.4:9904/api}
    image: teachermon-web:latest
    container_name: teachermon-web
    environment:
      NODE_ENV: production
    ports:
      - '9903:3000'
    depends_on:
      api:
        condition: service_healthy
    restart: always
    networks:
      - teachermon-net
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

networks:
  teachermon-net:
    driver: bridge
