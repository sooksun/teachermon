{
  "name": "@teachermon/database",
  "version": "1.0.0",
  "main": "./index.js",
  "types": "./index.d.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:resolve": "prisma migrate resolve",
    "db:fix-and-deploy": "prisma migrate resolve --applied 20260208151200_add_video_analysis && prisma migrate resolve --applied 20260209100000_update_indicators_v2 && prisma migrate resolve --applied 20260209120000_add_development_summary && prisma migrate resolve --applied 20260209180000_add_analysis_source_features && prisma migrate deploy",
    "db:push": "prisma db push",
    "db:seed": "tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^3.0.3",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
