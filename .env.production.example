# ============================================
# TeacherMon — Production Environment Variables
# ============================================
# คัดลอกไฟล์นี้แล้วเปลี่ยนชื่อเป็น .env.production
#   cp .env.production.example .env.production
#   nano .env.production
# ============================================

# ---- MySQL Database ----
MYSQL_ROOT_PASSWORD=CHANGE_ME_root_strong_password_here
MYSQL_USER=teachermon
MYSQL_PASSWORD=CHANGE_ME_app_strong_password_here

# ---- DATABASE_URL (Prisma format) ----
# ใช้ใน migration script — ภายใน Docker จะถูก override
DATABASE_URL="mysql://teachermon:CHANGE_ME_app_strong_password_here@localhost:3306/teachermon"

# ---- JWT ----
# สร้างด้วย: openssl rand -base64 48
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_48

# ---- Domain / CORS ----
DOMAIN=yourdomain.com
CORS_ORIGIN=https://yourdomain.com
NEXT_PUBLIC_API_URL=https://yourdomain.com/api

# ---- Gemini AI (optional) ----
GEMINI_API_KEY=

# ---- Backup ----
BACKUP_DIR=./backups
BACKUP_RETENTION_DAYS=30
