# TeacherMon Docker – Ubuntu 24
# Copy to .env and fill values before: docker compose -f docker-compose.ubuntu.yml up -d

# -----------------------------------------------------------------------------
# Database (Supabase)
# -----------------------------------------------------------------------------
# Direct connection: db.<project-ref>.supabase.co:5432
DATABASE_URL="postgresql://postgres.<PROJECT_REF>:YOUR_DB_PASSWORD@db.<PROJECT_REF>.supabase.co:5432/postgres"

# -----------------------------------------------------------------------------
# API
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=3001
API_PREFIX=api
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Web origin (for CORS). Use public URL in production.
CORS_ORIGIN=http://localhost:3000

# -----------------------------------------------------------------------------
# Web (Next.js)
# -----------------------------------------------------------------------------
# URL the browser uses to call API. Use public API URL in production.
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# -----------------------------------------------------------------------------
# Optional: AI (Gemini)
# -----------------------------------------------------------------------------
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.0-flash-exp
# AI_ENABLED=true
